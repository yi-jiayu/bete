environment: staging

ingress:
  hosts:
    - host: bete-staging.orario.gcp.jiayu.io
      paths:
        - /telegram/updates
  tls:
    - secretName: bete-tls
      hosts:
        - bete-staging.orario.gcp.jiayu.io
